import React, { FC } from 'react'
import styled from 'styled-components';

const EntitiesStyled = styled.section`

`;

class Entities extends React.Component {
    render() {
        return (
        <EntitiesStyled>
            Entities
        </EntitiesStyled>
        )
    }
}

export default Entities;